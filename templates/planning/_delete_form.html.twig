<form method="post" action="{{ path('planning_delete', {'idPlanning': planning.idPlanning}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ planning.idPlanning) }}">
    <button class="btn">Delete</button>
</form>
