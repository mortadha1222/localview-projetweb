{% extends 'front.html.twig' %}
{# {% extends 'base.html.twig' %} #}
{% block title %}Panier{% endblock %}

{% block body %}
    <h1>Panier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdPanier</th>
                <td>{{ panier.idPanier }}</td>
            </tr>
            <tr>
                <th>IdProduct</th>
                <td>{{ panier.idProduct }}</td>
            </tr>
            <tr>
                <th>IdUser</th>
                <td>{{ panier.idUser }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ panier.quantity }}</td>
            </tr>
            <tr>
                <th>Total</th>
                <td>{{ panier.total }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('panier_conroller_index') }}">back to list</a>

    <a href="{{ path('panier_conroller_edit', {'idPanier': panier.idPanier}) }}">edit</a>

    {{ include('panier/_delete_form.html.twig') }}
{% endblock %}
