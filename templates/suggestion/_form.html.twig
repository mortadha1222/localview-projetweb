{{ form_start(form,{'attr':{'novalidate':'novalidate'}}) }}
        {{ form_row(form.title,{'label':'Title','attr':{'placeholder':'Title', 'class': 'form-control'}}) }}
        {{ form_row(form.description,{'label':'Description','attr':{'placeholder':'Description', 'class': 'form-control'}}) }}
    <button class="btn btn-outline-success">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}

