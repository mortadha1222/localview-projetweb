<form method="post" action="{{ path('produits_delete', {'idProduct': produit.idProduct}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ produit.idProduct) }}">
    <button class="btn">Delete</button>
</form>
